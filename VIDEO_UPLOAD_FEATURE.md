# Функционал загрузки собственного видео

## Описание

Добавлена возможность загружать свои видео в бот для создания мемов с выбором аудио.

## Как использовать

### 1. Загрузите видео

Просто отправьте видео файл в чат с ботом.

### 2. Выберите способ добавления аудио

После загрузки видео бот предложит три варианта:

#### 🎲 Случайный трек
- Бот автоматически выберет случайный трек из плейлистов (`music_playlists.json`)
- Вырежет случайный фрагмент длительностью 12 секунд
- Добавит его к вашему видео

#### 📤 Загрузить свой аудио
- Вы можете загрузить свой аудио файл (MP3, WAV)
- Или отправить голосовое сообщение
- Бот извлечет случайный фрагмент длительностью 12 секунд

#### 🔍 Поиск по плейлистам
- Введите поисковый запрос (например: "phonk", "lofi", "drake")
- Бот найдет подходящие треки во всех плейлистах
- Выберите понравившийся трек из списка
- Бот скачает его и добавит к видео

### 3. Обработка видео

После выбора аудио:
- Бот обработает видео
- Добавит выбранное аудио
- Применит эффекты (масштабирование, обрезка под TikTok формат)
- Создаст миниатюру

### 4. Публикация

После обработки доступны те же опции, что и для обычных мемов:
- **Опубликовать** — опубликовать на все платформы
- **Выбрать платформы** — выбрать конкретные соцсети
- **Сменить трек** — заменить аудио на другой случайный трек
- **Сгенерировать заново** — создать новый вариант

## Технические детали

### Новые функции в `app/audio.py`

#### `search_tracks_in_playlists(music_playlists, search_query, max_results=10)`
Поиск треков в YouTube плейлистах по запросу.

**Параметры:**
- `music_playlists` — список URL плейлистов
- `search_query` — поисковый запрос
- `max_results` — максимальное количество результатов

**Возвращает:** список кортежей `(video_id, title, author)`

#### `download_specific_track(video_id, output_dir='audio', audio_format='mp3')`
Скачивает конкретный трек по video_id.

**Параметры:**
- `video_id` — ID видео YouTube или полный URL
- `output_dir` — директория для сохранения
- `audio_format` — формат аудио (mp3, wav)

**Возвращает:** путь к скачанному файлу

#### `extract_audio_from_file(input_path, output_dir='audio', audio_format='mp3')`
Извлекает аудио из видео или конвертирует аудио файл.

**Параметры:**
- `input_path` — путь к входному файлу (видео или аудио)
- `output_dir` — директория для сохранения
- `audio_format` — формат выходного аудио

**Возвращает:** путь к извлеченному/конвертированному файлу

### Новая функция в `app/service.py`

#### `process_uploaded_video_with_audio(video_path, audio_path=None, audio_duration=12, music_playlists=None, progress=None)`
Обрабатывает загруженное пользователем видео с добавлением аудио.

**Параметры:**
- `video_path` — путь к загруженному видео
- `audio_path` — путь к конкретному аудио (если пользователь загрузил свой)
- `audio_duration` — длительность аудио клипа в секундах
- `music_playlists` — список плейлистов для случайного выбора
- `progress` — callback функция для отображения прогресса

**Возвращает:** `GenerationResult` с путями к видео и миниатюре

### Обработчики в `bot.py`

#### `on_video_received(update, context)`
Обрабатывает загруженное видео, сохраняет его и предлагает выбор аудио.

#### `on_audio_received(update, context)`
Обрабатывает загруженный аудио файл или голосовое сообщение.

#### `on_callback_audio_choice(update, context)`
Обрабатывает выбор способа добавления аудио (случайный/загрузить/поиск).

#### `on_text_message(update, context)`
Обрабатывает текстовые сообщения для поиска треков.

#### `on_callback_select_track(update, context)`
Обрабатывает выбор конкретного трека из результатов поиска.

#### `process_video_with_selected_audio(update, context, video_path, audio_path=None, music_playlists=None)`
Координирует обработку видео с выбранным аудио и отправку результата.

## Рабочий процесс

```
1. Пользователь загружает видео
   ↓
2. Видео сохраняется во временную директорию
   ↓
3. Бот показывает кнопки выбора аудио
   ↓
4. В зависимости от выбора:
   
   a) Случайный трек:
      - Выбор случайного плейлиста
      - Загрузка случайного трека
      - Извлечение случайного фрагмента 12 сек
   
   b) Загрузить свой:
      - Ожидание аудио файла
      - Извлечение аудио (если это видео)
      - Конвертация в MP3
      - Извлечение случайного фрагмента 12 сек
   
   c) Поиск:
      - Ожидание поискового запроса
      - Поиск треков в плейлистах
      - Отображение результатов с кнопками
      - Загрузка выбранного трека
      - Извлечение случайного фрагмента 12 сек
   ↓
5. Обработка видео:
   - Применение эффектов
   - Добавление аудио
   - Масштабирование под формат TikTok (9:16)
   - Создание миниатюры
   ↓
6. Отправка результата с кнопками публикации
   ↓
7. Очистка временных файлов
```

## Особенности

- **Случайные фрагменты:** Аудио всегда вырезается случайным образом (12 секунд)
- **Поддержка форматов:** MP3, WAV, OGG, голосовые сообщения
- **Автоматическая очистка:** Временные файлы удаляются после обработки
- **История:** Обработанные видео добавляются в историю с возможностью публикации
- **Смена трека:** Можно заменить аудио после создания видео

## Примеры использования

### Пример 1: Случайный трек
1. Загрузите видео
2. Нажмите "🎲 Случайный трек"
3. Дождитесь обработки
4. Опубликуйте или смените трек

### Пример 2: Свой аудио файл
1. Загрузите видео
2. Нажмите "📤 Загрузить свой аудио"
3. Отправьте MP3 файл
4. Дождитесь обработки
5. Опубликуйте результат

### Пример 3: Поиск трека
1. Загрузите видео
2. Нажмите "🔍 Поиск по плейлистам"
3. Введите "phonk" (или любой запрос)
4. Выберите трек из списка
5. Дождитесь обработки
6. Опубликуйте результат

## Ограничения

- Максимальная длительность аудио фрагмента: 12 секунд
- Поиск ограничен 10 результатами
- Требуется `youtube_cookies.txt` для скачивания треков из YouTube
- Видео обрабатывается в формат 9:16 (TikTok/Reels)

## Требования

- `moviepy` — для обработки видео и аудио
- `yt-dlp` — для скачивания треков из YouTube
- `ffmpeg` — для кодирования видео
- Валидный `youtube_cookies.txt` файл

## Решение проблем

### "YouTube блокирует загрузку (403)"
Загрузите актуальный `youtube_cookies.txt` через команду `/uploadytcookies`

### "Видео не содержит аудио дорожку"
Если вы загрузили видео без звука, выберите один из способов добавления аудио

### "Ошибка при извлечении аудио"
Убедитесь, что загруженный файл является корректным аудио/видео файлом

### Треки не найдены в поиске
Попробуйте другой поисковый запрос или используйте случайный трек
