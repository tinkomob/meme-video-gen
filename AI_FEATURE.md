# AI Video Ideas Feature

## Обзор

Новая функция использует **Google Gemini AI** для генерации креативных идей для коротких вирусных видео на основе музыки. ИИ анализирует аудиотреки и предлагает яркие визуальные концепции для 8-секундных мем-видео.

## Установка

### 1. Установите зависимости

```powershell
pip install -r requirements.txt
```

Это установит `google-genai>=1.60.0` и все необходимые зависимости.

### 2. Получите API ключ Google Gemini

1. Перейдите на [Google AI Studio](https://ai.google.dev/)
2. Войдите с вашим Google аккаунтом
3. Создайте новый API ключ
4. Скопируйте ключ

### 3. Настройте .env файл

Добавьте в ваш `.env` файл:

```env
GEMINI_API_KEY=your_api_key_here
```

## Использование

### Способ 1: Команда /ai

#### Случайный трек из плейлистов

```
/ai
```

Бот:
1. Выберет случайный трек из `music_playlists.json`
2. Скачает его
3. Отправит ИИ для анализа
4. Вернёт креативную идею для видео

#### Поиск конкретного трека

```
/ai phonk
/ai lofi beats
/ai название трека
```

Бот:
1. Найдёт трек в плейлистах по запросу
2. Скачает первый подходящий результат
3. Сгенерирует идею для него

Если трек не найден, бот предложит:
- Попробовать случайный трек (`/ai`)
- Поискать другой трек

### Способ 2: Загрузка аудио файла

1. Отправьте аудио файл (MP3 или WAV) в чат
2. Бот покажет кнопку **"🤖 Сгенерировать идею для трека"**
3. Нажмите на кнопку
4. ИИ проанализирует ваш трек и предложит идею

## Примеры идей

Вот примеры идей, которые генерирует ИИ:

- **Для энергичного phonk трека:**
  > "Кот в костюме космонавта медленно летит среди парящих пицц"

- **Для lofi трека:**
  > "Человек серьёзно гладит невидимую собаку, камера в замедленной съёмке"

- **Для танцевального трека:**
  > "Танцующий хомяк в солнечных очках на фоне взрывающихся арбузов"

## Архитектура

### Новые файлы

#### `app/ai.py`

Модуль для работы с Google Gemini API:

- `generate_video_idea_from_track(track_title, track_artist, track_duration)` — генерация идеи по названию трека
- `generate_video_idea_from_audio_file(audio_path, track_duration)` — генерация идеи с анализом аудио файла

#### Обновлённые файлы

- `requirements.txt` — добавлен `google-genai>=1.60.0`
- `app/config.py` — добавлен `GEMINI_API_KEY`
- `bot.py`:
  - Новая команда `/ai`
  - Обновлён обработчик загрузки аудио
  - Добавлен callback handler для кнопки генерации идеи
  - Обновлён `HELP_TEXT`

### Использованные функции

Модуль использует существующие функции из `app/audio.py`:

- `search_tracks_in_playlists()` — поиск треков в плейлистах
- `download_specific_track()` — скачивание конкретного трека
- `download_random_song_from_playlist()` — скачивание случайного трека
- `get_song_title()` — получение названия трека
- `extract_audio_from_file()` — извлечение аудио из файлов

## Промпт-инжиниринг

ИИ использует специально настроенный промпт:

- **Роль:** Креативный режиссёр коротких вирусных видео
- **Длина:** 15-20 слов
- **Стиль:** Яркий, запоминающийся, смешной
- **Фокус:** Только визуальные образы (БЕЗ текста на видео)
- **Подход:** Неожиданные, абсурдные, ироничные образы

### Параметры модели

```python
model='gemini-2.0-flash-exp'
temperature=1.0  # Максимальная креативность
max_output_tokens=100
top_p=0.95
top_k=40
```

## Обработка ошибок

- **API ключ не настроен:** Бот покажет инструкцию по получению ключа
- **Трек не найден:** Предложит попробовать случайный трек или другой запрос
- **Ошибка скачивания:** Покажет детальное сообщение об ошибке
- **Ошибка ИИ:** Выведет сообщение и предложит проверить API ключ

## Ограничения

- **Длительность видео:** Идеи генерируются для 8-секундных видео
- **Формат идеи:** Одно предложение
- **Без текста:** Идеи фокусируются только на визуальном ряде
- **Квота API:** Проверьте лимиты на [Google AI Studio](https://ai.google.dev/)

## Примеры использования в коде

### Генерация идеи по названию трека

```python
from app.ai import generate_video_idea_from_track

idea = generate_video_idea_from_track(
    track_title="Lovely Bastards",
    track_artist="JVKE",
    track_duration=8
)
print(idea)
# "Кот в костюме космонавта медленно летит среди парящих пицц"
```

### Генерация идеи из аудио файла

```python
from app.ai import generate_video_idea_from_audio_file

idea = generate_video_idea_from_audio_file(
    audio_path="/path/to/audio.mp3",
    track_duration=8
)
print(idea)
# "Танцующий хомяк в солнечных очках на фоне взрывающихся арбузов"
```

## Тестирование

### Проверка настройки

```powershell
# В PowerShell
$env:GEMINI_API_KEY = "your_key"
python bot.py
```

В Telegram:
1. Отправьте `/ai`
2. Убедитесь, что бот генерирует идею

### Проверка с загруженным аудио

1. Отправьте MP3 файл в чат
2. Нажмите кнопку "Сгенерировать идею"
3. Убедитесь, что ИИ анализирует аудио

## Интеграция с workflow

Эта функция **не интегрирована** с генерацией видео автоматически. Она предоставляет только идеи.

Для полной автоматизации можно:
1. Получить идею через `/ai`
2. Вручную создать видео по идее
3. Использовать `/generate` для публикации

## Будущие улучшения

- [ ] Сохранение истории идей
- [ ] Автоматическая генерация видео по идее
- [ ] Поддержка разных длительностей видео
- [ ] Генерация нескольких вариантов идей
- [ ] Рейтинг идей от пользователей
- [ ] Интеграция с DALL-E для генерации изображений по идее

## Поддержка

Если возникли проблемы:

1. Проверьте `GEMINI_API_KEY` в `.env`
2. Убедитесь, что `google-genai` установлен
3. Проверьте логи в `errors.log`
4. Проверьте квоту API на [Google AI Studio](https://ai.google.dev/)

## Лицензия

Функция использует Google Gemini API, которая имеет собственные условия использования. Ознакомьтесь с [Google AI Terms of Service](https://ai.google.dev/terms).
